export default function NavbarLinkScrollClick(el) {
	console.log(el);
	// document.addEventListener('keydown', (event) => {
	// 	if (event.code === 'Enter' || event.code === 'Space') {
	// 		document.querySelector(el).click();
	// 	}
	// });
}
